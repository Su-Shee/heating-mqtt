version: '3.2'
services:
  mosquitto:
    image: toke/mosquitto
    network_mode: "host"

  service:
    build:
      context: ./service
      dockerfile: Dockerfile
    network_mode: "host"

  heater:
    build:
      context: ./heater
      dockerfile: Dockerfile
    network_mode: "host"

  sensors:
    build:
      context: ./heater
      dockerfile: Dockerfile
    network_mode: "host"
