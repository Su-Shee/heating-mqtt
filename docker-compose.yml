version: '3.2'
services:
  mosquitto:
    image: toke/mosquitto
    network_mode: "host"

  service:
    build:
      context: ./service
    network_mode: "host"

  heater:
    build:
      context: ./heater
    network_mode: "host"

  sensors:
    build:
      context: ./sensors
    network_mode: "host"
